= form_tag '', :method => :get, :class => 'form-inline' do
  = text_field_tag :start, params[:start], 'data-toggle' => 'datepicker'
  = text_field_tag :end, params[:end], 'data-toggle' => 'datepicker'
  = submit_tag t(:filter_sprint), :class => 'btn'
- if params[:start] and params[:end]
  = render 'burndown_graph'
