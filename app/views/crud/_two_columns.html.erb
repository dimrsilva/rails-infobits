<p>
  <%= link_to 'Create Person', new_contact_person_path, :class => 'btn' %>
  <%= link_to 'Create Company', new_contact_company_path, :class => 'btn' %>
</p>
<div class="row">
  <div class="span6">
    <%= render 'table_list' %>
  </div>
  <div class="span6">
    <div class="well">
      <% if @row == nil %>
        No row selected
      <% else %>
      <%= render 'form' %>
      <% end %>
    </div>
  </div>
</div>
